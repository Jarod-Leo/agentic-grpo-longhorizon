# 长程工具 Agent 的 OPD 后训练项目：首版精简方案

## 1. 项目定位与叙事线
每组详细实验规划和细节可以看“实验计划首版.md”,下面是对首版的精筛。
**目标：在 1–2 周、最多 80 GPU-hours 内，完成一个面向大模型算法／后训练岗位、结果可信且可复现的项目**。以 OPD 为主线，OPSD 留作有条件的后续扩展。

建议项目名称：

**面向长程工具调用 Agent 的 On-Policy Distillation：训练信号、任务可靠性与计算成本分析**

叙事围绕三个问题展开：

1. **问题定位**：多轮工具任务只有终局成败反馈，GRPO 在组内全成功或全失败时缺少结果奖励的相对学习信号。
2. **方法实现**：让冻结教师在学生实际生成的交互历史上提供 token 级监督，比较环境奖励、教师监督及两者联合。
3. **证据闭环**：用独立任务成功率、重复执行可靠性、错误类型和计算成本，判断蒸馏是否值得使用。

“全失败组中存在蒸馏信号”只是机制证据；必须观察任务表现，才能声称训练有效。不预设联合方法一定最好。

你的贡献明确为：**多轮工具场景下的 OPD 接入、训练与评测正确性、受控实验和诊断分析**。PRM-Lite、LATA 归于原项目，不包装成个人原创。

## 2. 实验矩阵：保留五组，移出其余分支

| 首版实验 | 对应原计划 | 作用 |
|---|---|---|
| 原始 Qwen3-8B | E00 | 修复后能力起点，仅评估 |
| Vanilla GRPO | E01 | 只依靠终局奖励的基线 |
| GRPO＋PRM-Lite＋LATA | E05 | 原项目代表性改进对照 |
| 纯 OPD | E06 | 判断教师监督本身是否有效 |
| GRPO＋OPD | E07 | 判断教师监督与任务奖励是否互补 |

四个训练分支全部从官方 Qwen3-8B 和相同 LoRA 初始化开始，不从历史单步 checkpoint 续训。联合 OPD 首版只接 Vanilla GRPO，不再搜索“最优 RL 配方”后继续堆叠。

**从首版移出：**

- E02–E04：TD、PRM、LATA 的逐模块复现。
- E08–E10：最优 RL＋OPD、两种训练顺序。
- E11–E14：OPSD 全套分支和教师切换。
- 大规模蒸馏系数扫描、刷新频率、组大小、完整分布／top-k 蒸馏消融。
- 选择性蒸馏、100–300 个新任务生成、retail 迁移、跨模拟器复测和额外 SFT。

**首版新增的必要验证：**

- 修复后完整工具定义进入实际模型输入。
- 可靠性指标的正确实现。
- 教师是否比学生更擅长当前任务。
- token 对齐、角色 mask、教师冻结及梯度方向检查。
- 全失败组中，GRPO 与 OPD 的信号覆盖情况。

这些验证比增加更多算法组合更能支撑结论。

## 3. 方法与实现边界

### 共用设置

- 学生：Qwen3-8B，关闭思考，LoRA rank=16、alpha=32，学习率 `5e-6`，micro-batch=1。
- 用户模拟器：MiMo；沿用已有角色提示、限流、错误传播和轨迹隔离。
- 每次更新：4 个任务，各 8 条轨迹；保留已验证的长度和轮数限制。
- 四个分支使用相同任务调度、训练种子、数据预算和优化器设置。
- 新主矩阵统一关闭额外 reference KL 和熵奖励，单独比较任务奖励与蒸馏目标；因此历史测速只作参考，不充当新基线。
- 为避免截断采样与评分分布混用，首版训练统一使用 temperature=1、top_p=1、关闭 top_k；评分使用相同的完整 softmax 分布。评测统一使用 temperature=0.7、top_p=0.9。

### OPD 接入

候选教师为本地 **Qwen3-32B BF16**，学生和教师各占同节点一张 GPU。先验证单卡显存、最长输入和 tokenizer／词表兼容性；MiMo 只负责模拟用户。

在现有 vendored verl 中增加最小接入，不进行整套框架升级：

- 教师评分器接收学生实际 token 序列，返回逐 token log-prob。
- batch 增加与学生 response 对齐的教师 log-prob。
- 增加独立的 sampled-token PG-OPD 损失及配置开关，支持纯 OPD、GRPO＋OPD。
- 只训练 assistant 文本和工具调用；用户消息、工具返回、padding 不参与损失。
- 教师冻结，评分严格因果；固定批内学生版本，采样和评分完成后再更新。
- 不把教师信号压成整条轨迹的标量奖励，也不对 OPD 信号做 GRPO 组内标准化。

联合目标为：

$$
\mathcal L=\mathcal L_{\mathrm{GRPO}}+0.3\,\mathcal L_{\mathrm{OPD}}.
$$

首版固定系数，不展开超参数网格；分别记录两个分支的梯度尺度。教师仅生成更好的回答再做 SFT，不属于这里的 token 级 OPD。[verl 官方定义与实现说明](https://verl.readthedocs.io/en/latest/algo/opd.html)

若 32B 无法在限定资源内评分，或教师能力预检没有显示指导价值，停止 OPD 长跑并报告原因，不静默换教师或改变方法名称。

## 4. 数据、评测与验收

### 数据协议

按 2026-09-20 确认的协议，沿用原实验的 **40 train／10 test**，不单独划分 dev。唯一划分来源为 `agentic-grpo-longhorizon/experiments/sft_collect_airline/split.json`：`seen_task_ids` 对应 train，`unseen_task_ids` 对应 test；不重新抽样任务。

所有新分支重新从官方模型开始，不加载历史 SFT、GRPO 或单步 checkpoint。复用现有 `build_grpo_parquet.py` 构造数据：训练只使用原 40 个 seen 任务。训练中的诊断评测只使用 train；现有包含全部 50 个任务的 `val.parquet` 仅用于明确标注 train/test 的完整评测，不作为独立验证集，也不用于选择 checkpoint。

test 是本轮训练留出的任务集合；旧项目曾评测过这些任务，需如实记录历史曝光，不能宣称项目历史上从未使用。此划分是任务 ID 留出，不保证不同集合的流程家族完全独立。

已完成 E00（Job 162198）的原始 30 train／10 dev 划分、逐任务结果和验收记录保留，作为历史运行快照。两组任务合并后恰好覆盖恢复后的 40 个 train 任务，可直接从逐任务成功次数重新汇总，不需要重新采样；test 尚未评测。按现行协议重汇总的 E00 train 结果如下：

| 集合 | 任务数 | 轨迹数 | 成功数 | $\mathrm{pass}^{1}$（任务成功率） | $\mathrm{pass}^{4}$（四次全部成功） | $\mathrm{pass@4}$（四次至少一次成功） |
|---|---:|---:|---:|---:|---:|---:|
| train | 40 | 320 | 41 | 12.8125% | 4.3571% | 24.0357% |

E00 目录下的旧划分和启动入口仅用于追溯该次运行；后续分支使用上述原实验的 40/10 划分，不复制旧的 dev 协议。

### 评测协议

- 修复现有评测器中 `pass@k` 与 `pass^k` 的混用，增加已知成功次数的单元测试。
- 主指标：$\mathrm{pass}^{1}$（任务成功率，即任务宏平均成功率）；可靠性指标为 $\mathrm{pass}^{4}$（四次全部成功），补充 $\mathrm{pass@4}$（四次至少一次成功）。分别报告 train 和 test，错误率、轮数耗尽率、token 数与耗时作为诊断及成本指标。
- 训练前与预算中点在相同 train 任务上每任务评估 4 次，仅用于训练诊断；已有 E00 每任务 8 次的基线可复用，并明确标注重复次数。正式比较使用预先冻结的统一训练预算末 checkpoint，不再按 dev 或 test 成功率挑选 checkpoint。最终 train 每任务评估 8 次。
- 全部方法与选择规则冻结后，test 每任务评估 8 次；不根据 test 结果返调。
- 预先固定为 Vanilla GRPO（E01）和 GRPO＋OPD（E07）增加第二个训练种子，不根据 test 成绩挑选追加分支。其他分支明确标注单种子。
- 报告任务级配对区间和种子差异；10 个 test 任务只支持有限结论，80 条重复轨迹不能当成 80 个独立任务。

### 开跑前的阻断检查

1. 真实工具输入保留嵌套 `items`、`required`；非法参数能被完整审计发现。
2. 同权重、同输入、同计算路径的师生评分一致；位置错一位或混入工具 token 的测试必须失败。
3. 教师无梯度，学生有预期方向的梯度；蒸馏系数为零时回到对应 RL 路径。
4. 全失败组可以没有 GRPO outcome advantage，但蒸馏路径仍正常工作。
5. PRM-Lite＋LATA 的有效权重和梯度保持有限；若原实现数值异常，记录故障，不将修正后的新版本冒称为原版。
6. 单步更新、LoRA 权重变化、checkpoint、资源释放均通过。

教师能力预检使用 train 任务、每任务 4 次及相同的评测推理配置，并人工核对训练任务中的失败状态样例；不使用 test 选择教师。不能仅凭“32B 大于 8B”认定教师有效。

## 5. 执行预算、交付物与下一版

**总上限 80 GPU-hours，包含教师等待、加载、训练、评测和失败重试。**

- 最多 10 GPU-hours：修复后回归、教师能力／显存预检、三步 OPD 测速。
- 预留 15 GPU-hours：统一评测和故障余量。
- 最多 55 GPU-hours：四个训练分支及两个关键方案的第二种子。

正式训练预算从 **50、30、20 步**中选择：以三步测速的较保守步时估算，选择能让全部上述训练落入 55 GPU-hours 的最大档位，并在比较开始前统一冻结。如果连 20 步都超预算，先交付可行性结果，不只缩短某个不利分支。

建议节奏：

- **第 1–3 天：**冻结数据与指标，完成教师及评分正确性预检。
- **第 4–7 天：**完成四分支短训，检查信号、错误类型和成本。
- **第 8–10 天：**完成关键第二种子和最终测试。
- **第 11–14 天：**整理技术报告、模型卡和轨迹展示，留出排队与调试余量。

首版交付：

- 一套公共配置及四个训练入口、独立评测入口。
- 五行主结果表、学习曲线、错误分布和成本分解。
- token 评分、角色 mask、损失路径的实现说明与测试证据。
- 最终 LoRA、模型卡、同任务成功／失败轨迹回放。
- 清楚区分上游方法、个人实现和实测增量的 README／简历描述。

简历叙事使用“实现了什么、验证了什么、结果及限制是什么”，提升数字在最终测试后填写；若 OPD 未优于 GRPO，保留负结果和原因分析，不补造单调提升的故事。

**OPSD 的进入条件：**OPD 已验证教师信号有用，且证据显示教师成本值得进一步优化，同时能构造可信的环境反馈。下一版只从“GRPO＋反馈条件自蒸馏”一个分支开始，不恢复原来的整套大矩阵。该方向是工具场景适配，不能直接沿用数学推理论文的效果结论。[OPSD 原论文](https://arxiv.org/abs/2601.18734)


### 2026-09-21 E01 执行预算更新

用户明确批准 E01 正式训练 **200步**，采用已通过单步验证的去padding与动态微批32768；每100步进行train集独立评测（40任务×8次）并保存完整checkpoint，完成后归档HDD长期保留。此决定替代上文对E01的20/30/50步候选安排；其余实验预算与全矩阵80 GPU-hours约束需后续重估，不据此宣称全矩阵仍符合原预算。测试集仍留到最终比较。

2026-09-21 故障修复后，用户将 E01 完整checkpoint频率调整为 **每50步**（50/100/150/200均归档HDD），独立评测仍为每100步。MiMo的可选usage缺失不再中断有效对话，计费用量保持未知标记；详见 `agentic-grpo-longhorizon/docs/e01_mimo_failure_analysis.md`。


### 2026-09-22 E05 执行预算更新

用户确认下一项 **GRPO＋PRM-Lite＋LATA（E05）同样训练200步**，每50步保存完整checkpoint并归档HDD，每100步评测40个train任务×8条。模型、LoRA初始化、数据、采样、性能配置与E01保持可比；test保留至最终矩阵评测。PRM-Lite系数0.3、LATA alpha1.05沿用已有实现，LATA位置权重/长度缩放的实际语义及等价数值修复在 `agentic-grpo-longhorizon/docs/e05_prm_lite_lata_plan.md` 中明确说明。

此决定替代上文对E05的20/30/50步候选。E01/E05同时使用单个100RPM MiMo额度时不能各按独占额度并发采样；新分支先完成CPU验证，真实单卡smoke在E01释放额度后运行，成功后自动放行200步作业。全矩阵原80GPU小时预算仍需依据新实测重新核算。


### 2026-09-22 蒸馏分支实施范围更新

用户明确要求按E06→E07→E11→E12顺序实现，仅做CPU测试，暂不做GPU测试。E06纯OPD与E07 GRPO＋OPD已分别由CPU作业166809/166814验收；之后恢复首版计划中的E11纯Agent-OPSD-FB和E12 GRPO＋OPSD代码准备。此授权替代先前“不实现OPSD”的范围约束，但不等同于已满足教师质量条件，也不授权GPU训练。各分支详细实施与验收计划见agentic-grpo-longhorizon/docs/。

四组配置暂沿用现行200步、4×8 rollout、50步归档HDD、100步train评测默认值，便于后续可比实验；正式GPU启动、教师能力、显存与步时尚需另行验证。E07联合系数0.3；E12自蒸馏系数在其实施阶段记录，不通过test选择。

E11已由CPU作业166829通过153项回归，之后开始E12联合实现。E12暂定目标为GRPO＋0.3×OPSD，复用相同F2反馈和更新前自教师；0.3仅为未调优默认值，不表示信号尺度已与外教师相当。CPU阶段结束后，教师价值和真实训练仍属待验证。

四组CPU实施现已依序验收完成：E06/166809（127项）、E07/166814（129项）、E11/166829（153项）、E12/166837（155项，包含共用回归）。所有本轮Slurm验收均为0GPU，尚未进行这四组GPU测试或训练。详细结果和实现边界见各组docs/*_cpu_plan.md及experiments/*/cpu_validation.json。
